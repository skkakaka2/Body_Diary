spring:
  application:
    name: body-diary
  devtools:
    restart:
      enabled: true
      additional-paths: [ src/main/java ]
      exclude: [ src/main/resources/static ]
  
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/body_diary?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8088